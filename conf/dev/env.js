const {clientGlobal, serverGlobal, clientPublicPath, proxy, server, mock} = require('../env.base');

module.exports = {
  clientGlobal,
  serverGlobal,
  clientPublicPath,
  proxy,
  server,
  mock,
};
